# Documentation

Workflow guides live in `docs/public/`. Component READMEs remain next to
their code because they document local APIs, tools, and build entrypoints.

Start with `docs/public/README.md` for the complete documentation index.
